# sms-led-moderation-api
AWS Lambda that serves moderation endpoints (settings, pending/approved/live messages, approve/reject, etc.).

Deploy by zipping the contents of this folder (not the repo root) and running `aws lambda update-function-code` as documented in the root README.
